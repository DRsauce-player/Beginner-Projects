#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    int length, prev = 0, curr = 1, temp;

    cout << "Length: ";
    cin >> length;
    cout << "Fibonacci Sequence up to " << length << " numbers.\n";

    for (int i = 0; i < length + 2; i++)
    {
        if (i < 2)
        {
            for (int j = 0; j < i; j++)
            {
                cout << i;
            }
            cout << endl;
        }

        else
        {
            cout << prev + curr;
            temp = curr;
            curr = curr + prev;
            prev = temp;
            cout << endl;
        }
    }

    system("pause>0");
}
